# VPN-protocol-26
VPN protocol to keep ahead of restrictions in highly restricted areas in the world.

## QR Scanner (macOS)

Native macOS QR code scanner with Continuity Camera support (USB‑C iPhone as camera). See [`QRScanner/README.md`](QRScanner/README.md).

```bash
cd QRScanner && ./install.sh   # run on a Mac with Xcode installed
```
